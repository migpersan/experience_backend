<?php 
/**
 * Implementing hook_theme().
 */
function formulario_contacto_theme($existing, $type, $theme, $path) {
    return array(
        'vista_formulario_contacto'=>array(
            'variables' => array('items' => array(), 'title' =>'')
        )
    );

}