<?php 
/**
 * Implementing hook_theme().
 */
function anadir_libro_theme($existing, $type, $theme, $path) {
    return array(
        'form_anadir_libro'=>array(
            'variables' => array('items' => array(), 'title' =>'')
        )
    );

}